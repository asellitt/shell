# Header definitions
if [ -f ~/.header ]; then
    . ~/.header
fi

# Function definitions
if [ -f ~/.bash_functions ]; then
    . ~/.bash_functions
fi
if [ -f ~/.functions ]; then
    . ~/.functions
fi

# Alias definitions
if [ -f ~/.bash_aliases ]; then
    . ~/.bash_aliases
fi

